# JavaScript
A code repo for JavaScript series at Chai aur Code youtube channel

## Git Workflow

Follow this flow whenever you want to update the code and push your changes to the repository:

```text
1. git pull --rebase origin main
          ↓
2. Edit your code
          ↓
3. git add .
          ↓
4. git commit -m "Added new JavaScript logic"
          ↓
5. git push